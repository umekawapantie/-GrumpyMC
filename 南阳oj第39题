#include<stdio.h>
#include<math.h>
int main()
{
	int n;
	scanf_s("%d", &n);
	int h, t, s;
		h = n / 100;
		t = (n - h * 100) / 10;
		s = n % 10;
	if (pow(h, 3) + pow(t, 3) + pow(s, 3) == n)
	{
		printf("yes");
	}
	else
	{
		printf("NO");
	}
}
