#include<stdio.h>
int main()
{
	int t;
	scanf("%d",&t);
	while(t--)
	{
		int f=0,max=0;
		int a,b,sum;
		for(int i=1;i<=7;i++)
		{
			scanf("%d%d",&a,&b);
			sum=a+b;
			if(max<sum)
			{
				flag=i;
				max=sum;
			}
		}
		if(max<=8)
		f=0;
		printf("%d\n",f);
	}
	return 0;
 } 
